#include <stdio.h> //include standard input and output library
// include tells the compiler to include the header file

// int -->(return type)  main(function parameter) --> function
/*  we must nedd main() in function C programs in a hosted environment 
    (like those running on an operating system) there are excpetions*/
int main(){ // main function where the acctual code will be written 
    // printing syntax printf(const char *__format, ...)
    printf("Hello Word"); // must use a semi colon for statement terminator

    return 0; // return 0 for succesful program execution
}

// NOTE : Return type of main function is integer ( we will return 0 for succesful execution)
